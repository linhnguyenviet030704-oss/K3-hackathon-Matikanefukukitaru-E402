# Install PyTorch FIRST from the CUDA wheel index — plain `pip install torch` gives the
# CPU-only build on Windows:
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
#
# Then: pip install -r requirements.txt
#
# Swap cu126 -> cu118 for older drivers, or drop the --index-url for CPU-only.

torch>=2.4
torchvision>=0.19
pillow>=10.2
timm>=1.0.9              # MobileNetV4 only; the ResNet-50 path is pure torchvision
huggingface_hub>=0.24    # hf_download.py
